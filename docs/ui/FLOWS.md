# Flows

Stub — to be written in Phase 6 (UI implementation).

## Planned flows

- Extraction flow: select compound → start → tick countdown → reveal → vault
- Synthesis flow: select pair → lab check (optional) → start → tick → reveal → vault
- Joint synthesis flow: stage → partner joins → matched → both tick → both reveal
- Market flow: select compound → set price → pay listing fee → wait → receive proceeds
- Patent challenge flow: alert → view window → synthesize in time → claim/lose
- Prestige flow: check requirements → request → review blockers → confirm → reset
- Offline income flow: join → server settles → delivery animation → balance update
